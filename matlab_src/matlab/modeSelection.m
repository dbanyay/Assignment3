function decisions = modeSelection(blocks)
% Select encoding mode for each 16*16 block

numframes = size(blocks,4);
numblocks = size(blocks,3);


curblock = blocks(:,:,1,1);
prevblock = blocks(:,:,1,2);






end

